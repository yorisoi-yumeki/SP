# SP軍団シフト

SP軍団（9名のマッチングビジネスチーム）の月次シフト自動割当ツール。
「勉強会」（メイン/サブあり）と「M」（マッチングイベント、メインのみ）の
2種類のシフトを、複数ルールに基づいて自動で割り当てる。

## 公開ページ（実際に使う場所）

このツールは Claude の Artifact（`db` 機能つきの共有Webページ）として公開されている。
9名はこのリポジトリではなく、以下のリンクを開いて使う。

- 公開URL：https://claude.ai/artifact/NWExvWvfrHcwcM2T7vpt5f
- 共有範囲：オーナーの組織内のみ（Artifact自体の「共有」メニューから追加設定が必要）

## このリポジトリの役割

ソースコードのバックアップ・バージョン管理用。`source/sp-shift-maker.html` が
公開ページと同じ内容の単一HTMLファイル。修正する場合はこのファイルを編集し、
Claude Codeで同じ内容を上記Artifactへ再公開（republish）する。

```
source/sp-shift-maker.html
  ├─ 見た目・入力フォーム一式（HTML/CSS）
  └─ ロジック一式（JavaScript、無依存）
       ├─ ruleDefault()        … メンバー×日程ごとの◯/△/×判定
       ├─ runAssignment()      … バランシング（貪欲法）による自動割当
       ├─ parseScheduleBulk()  … 月間予定テキストの一括貼り付け解析
       ├─ parseM20Progress()   … M20進捗テキストの解析
       └─ subscribeWithRetry() … 共有DBの購読（エラー時は自動再接続）
```

## 経緯（前バージョンからの引き継ぎ）

前バージョン（Cowork上のArtifact）で「共有DBへの書き込みは成功するのに、
ブラウザ画面に反映されない」という不具合が発生し、原因を特定しきれないまま
Claude Codeへ引き継がれた。

原因として最も可能性が高かったのは、共有DBのリアルタイム購読
（`onSnapshot`）が仕様上「一度エラーで切断すると自動では復活しない」ことと、
前バージョンのコードがそのエラーを拾う処理を空にしていた（`function(){}`）ため、
切断に気づけないまま画面が固まっていたこと。

このバージョンでは対策として、

1. 購読エラーを検知したら画面にバナー表示し、自動で再接続を試みる
   （`subscribeWithRetry()`）
2. 操作した本人の画面は、保存の完了を待たずにその場で即座に更新する
   （楽観的更新。他の共有ツールで実績のある方式）

の2点を追加した。公開直後に実際の書き込み・読み出しを外部から確認済み。

## 業務ルール（確定仕様）

### 対象者（9名）
だいち、つよぽん、まぎー、やまぴー、よう、りん、ねぎ、とよかず、こばぴー

### 用語
- **勉強会**：メイン（担当・その回には参加できない）とサブ（参加しつつ補助）の役割がある。
- **M**：マッチングイベント。メインのみで、サブの概念は存在しない。
- **◯／△／×**：◯＝入れる（希望）、△＝できれば避けたい（優先度を下げて割当）、×＝不可（除外）。

### ルール一覧
1. よう・つよぽん：メインは同日重複不可
2. まぎー・こばぴー：2nd種別を含む日はメイン不可
3. とよかず・やまぴー：飲み会・カフェ会・食事会（CLC Final含む）が単体開催の日のみ対象。
   **単体開催の飲み会は2人とも一緒に入ってよい**（3人体制）。**複合日の飲み会**
   （他の種別と組み合わさった日）は2名＋とよかず/やまぴーから最大1名の＋1枠のみ。
   カフェ会・食事会は通常の2名のメイン枠を他メンバーと取り合う（＋1枠は無い）。
   サブには一切入らない
4. CLC：メインは前月からの2ヶ月連続不可（`history/clc`に自動記録）
5. Enトレ2：その月の対象者はメイン不可
6. カフェ会：メインはM20チャレンジ中の人のみ対象
7. CLC Final：3ヶ月ごと（8月/11月/2月/5月…）に食事会が改称したもの。食事会と同ルール
8. 前月・前々月同種別メイン回避：ソフトルール（必須ではない）。可否表では
   前月＝赤背景、前々月＝黄背景で表示し、セルの下にどの種別が一致したのか
   （例：「フェス」）を常時表示する（`resultsHistory`の該当月から判定。
   CLCの2ヶ月連続禁止ルール＝ハード制約とは別物なので混同しないこと）
9. 合計バランス：勉強会メイン＋M入力数を9名で均等化（サブは別枠）

## 結果の確定・共有

「今月のシフト」の「この結果をコピー」ボタンで、LINE等にそのまま貼り付けられる
テキスト（人数などの内部情報を含まない、そのまま送れる形式）をクリップボードに
コピーする。表右端の「回数(確認用)」列は各人の今月の合計回数（メイン＝勉強会
メイン回数／サブ＝サブ回数）を示す確認用の参考値で、コピーする内容には含まれない。
コピーと同時に、来月の判定に必要な情報（CLCの前月メイン・`resultsHistory`）も
記録する（＝コピー操作が「確定」を兼ねる。ドラフトを確認したいだけで送るつもりが
ない場合は、コピーせず自動生成のやり直しにとどめること）。

## Mシフト必要回数（自動算出）

①の設定に「M開催回数」「1回あたりの人数」を入力すると、総M枠数（開催回数×人数）を
求め、④で自動生成した勉強会メイン+サブの実績をもとに、9名の負荷が均等になるよう
水準均等化（water-filling）アルゴリズムで配分した「Mシフト必要回数」を表示する
（`computeMTargets()`／`roundPreservingSum()`）。これは「今月のMシフト回数」欄に
何を入力するかの参考値であり、自動でMのシフト自体を組むものではない。

## 入力完了チェック

③可否表の最終行に、9名それぞれの「入力完了チェック」ボタンがある。押すと
`state/config.inputCompleted`に本人名が記録され、緑色（完了）に変わる。
③の見出し横のサマリーにも「入力完了 n/9」として表示される。

## 一括登録時のメイン自動反映

②の一括貼り付けで、日程テキストの venue（【荏原】等）直後の行がメンバー名
だけの読点区切りだった場合、その人を③可否表の該当日へ自動で◯反映する
（`parseScheduleBulk()`が`mainNames`として抽出）。既にメインが決まった
状態からこのツールを使い始めるケースを想定した機能。既存の可否入力が
あれば上書きせずマージする（サブの人名は従来通り無視）。

## 未解決事項（次にやること）

- Enトレ2対象者：今月分が未入力
- M20チャレンジ中リスト：進捗テキストの解析結果は保存済みだが、
  「自動判定して反映」ボタンがまだ押されていない
