# セッション引き継ぎ書

最終更新：2026年9月22日
次のセッションは、**まずこのファイルを読んでから** `README.md` にあたってください。

---

## 1. プロジェクト概要

「SP軍団シフトメーカー」＝SP軍団（9名のマッチングビジネスチーム）の月次シフト
自動割当ツール。「勉強会」（メイン/サブあり）と「M」（マッチングイベント、
メインのみ）の2種類のシフトを、複数ルールに基づいて自動で割り当てる。

```
公開ページ（実際に9名が使う場所）
  https://claude.ai/artifact/NWExvWvfrHcwcM2T7vpt5f  （現在 Version 9）

このリポジトリ（ソースのバックアップ・バージョン管理用）
  https://github.com/yorisoi-yumeki/SP
  ローカルクローン：/home/user/sp
  本体ファイル：source/sp-shift-maker.html（単一HTML、無依存）
```

**重要**：このツールは `douga`（YouTubeチャンネル制作）リポジトリとは
無関係の別プロジェクト。間違えて `douga` 側で作業しないこと。

修正の流れは毎回同じ：
1. `source/sp-shift-maker.html` を編集
2. jsdomで動作確認（下記「開発・テストの方法」参照）
3. `Artifact` ツールで `url: https://claude.ai/artifact/NWExvWvfrHcwcM2T7vpt5f`
   を指定して再公開（republish）
4. `git add/commit/push`（このリポジトリへ）

---

## 2. 現在の進捗

### 完了していること
- 前バージョン（Cowork Artifact）の「書き込みは成功するが画面に反映されない」
  不具合を修正（`subscribeWithRetry()`で自動再接続、楽観的更新を全書き込みに追加）
- 業務ルール1〜9をすべて実装・jsdomで検証済み
- 2026年08月・09月の実績データ（`resultsHistory`）を実データで登録済み
  （前月＝赤枠／前々月＝黄枠の判定に使用）
- 2026年10月の日程10件を登録済み（実データ、付録Bベース）
- 「この結果をコピー」でLINE等に貼り付けられるテキストを生成し、
  同時に来月判定用の情報（CLC履歴・resultsHistory）を記録する機能
- 「Mシフト必要回数」を水準均等化（water-filling）で自動算出し、
  常時表示の「バランス確認」カードに表示
- 「入力完了チェック」行（可否表の最終行、9名分）
- 一括登録（②）で、venue直後にメイン名があれば可否表へ◯自動反映

### 未完了・要確認（最優先）
- **⚠ 共有範囲の設定が未完了**：上記公開URLは「オーナーのみ閲覧可」の
  ままで、他の8名がまだ開けない状態。ユーザーに複数回お願いしているが
  完了確認が取れていない。次のセッション冒頭で確認すること
- **合計回数の偏りについて、ユーザーへの回答待ち**：とよかず・やまぴーの
  勉強会メイン+サブ合計が他の7名より大幅に少ない（構造上必然。Mシフトで
  補う設計）とユーザーに説明済みだが、「この認識で合っているか／7名同士の
  差ももっと詰めたいか」への返答がまだ来ていない

---

## 3. 次にやるべきこと

1. 共有範囲の設定が完了したか確認
2. 「合計回数の偏り」についてのユーザーの回答を待ち、必要なら
   バランシングアルゴリズム（`runAssignment()`内のsort比較関数）を調整
3. 未入力項目（下記「4. 維持すべき仕様」の末尾を参照）の入力サポート

---

## 4. 維持すべき仕様（変更時は要注意）

### 対象者（9名）
だいち、つよぽん、まぎー、やまぴー、よう、りん、ねぎ、とよかず、こばぴー

### ルール一覧（確定・実装済み）
1. よう・つよぽん：メインは同日重複不可
2. まぎー・こばぴー：2nd種別を含む日はメイン不可
3. **とよかず・やまぴー**：勉強会は「飲み会・カフェ会・食事会（CLC Final含む）が
   単体開催」の日のみ対象。
   - **単体開催の飲み会**：2人とも一緒に入ってよい（3人体制／メイン枠3）
   - **複合日の飲み会**（他種別と組み合わさった日）：2名＋とよかず/やまぴー
     から最大1名の＋1枠のみ（実績データ準拠。標準化しないこと—ユーザーが
     明示的に「複合日は単体と違う」と確認済み）
   - **カフェ会・食事会**：通常の2名のメイン枠を他メンバーと取り合う
     （＋1枠は無い）
   - サブには一切入らない。勉強会シフト数が少ない分、Mシフトに多く入る
4. CLC：メインは前月からの2ヶ月連続不可（`history/clc`に自動記録。ハード制約）
5. Enトレ2：その月の対象者はメイン不可
6. カフェ会：メインはM20チャレンジ中の人のみ対象
7. CLC Final：3ヶ月ごと（8月/11月/2月/5月…）に食事会が改称。食事会と同義
   （`TYPE_ALIASES`でエイリアス処理。CLCの連続禁止ルールとは無関係）
8. 前月・前々月同種別メイン回避：ソフトルール。可否表で前月＝赤背景、
   前々月＝黄背景（CLCのハード制約とは別物。混同しないよう明示すること）
9. 合計バランス：勉強会メイン＋M入力数を9名で均等化（サブは別枠で均等化）

### UIの設計方針（ユーザーから明示的に指摘された制約）
- **明示的に依頼されていない機能・表示を勝手に追加しない**。過去に
  「個別追加/削除フォーム」「可否表セル下の小さい注記」を無許可で追加し、
  ユーザーから強く指摘され削除した経緯がある
- 日程の追加は②の一括登録のみ（個別追加フォームは意図的に削除済み。
  復活させない）
- 「今月のシフト」表と「回数(確認用)」は完全に分離（表の列に回数を
  混ぜない。コピーテキストにも回数を含めない）

### 技術的な設計判断（再発防止のため維持すること）
- 全onSnapshot購読は`subscribeWithRetry()`経由（エラー時に自動再接続、
  空のエラーハンドラを書かない）
- 全DB書き込みは楽観的更新（ローカルstateを先に更新してrender()し、
  その後DBへ書き込む）
- ライトモード固定（ダークモード対応は意図的に削除済み。再度追加しない）
- 常にライトモード：`color-scheme:light`

---

## 5. 開発・テストの方法

DOM/db機能を含む変更は、公開前に必ずjsdomでシナリオテストすること
（このセッションで繰り返し使った手法）。

```bash
# 1. jsdomがなければ /tmp にインストール
cd /tmp && npm install jsdom --no-save --silent

# 2. sp-shift-maker.htmlの<script>部分だけ抜き出し構文チェック
node -e "
const fs = require('fs');
const html = fs.readFileSync('/home/user/sp/source/sp-shift-maker.html', 'utf8');
const m = html.match(/<script>([\s\S]*)<\/script>/);
fs.writeFileSync('/tmp/check.js', m[1]);
"
node --check /tmp/check.js

# 3. window.claude.use('db')をfakeDb(インメモリ実装)に差し替えて
#    jsdomでページを実際に動かし、可否表・自動生成結果・コピー内容などを
#    document.querySelector等で検証する。fakeDbの実装例は
#    このセッションのやり取り（過去のtest-sp-shift.js等）を参照
```

公開・反映の手順：
```
Artifact({ action: "publish", url: "https://claude.ai/artifact/NWExvWvfrHcwcM2T7vpt5f",
           file_path: "/home/user/sp/source/sp-shift-maker.html" })
```

本番DBを直接確認・修正する場合は`ArtifactData`ツール（get/set/update/list等）を使う。
